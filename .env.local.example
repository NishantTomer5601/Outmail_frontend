# Frontend Environment Variables
NEXT_PUBLIC_BACKEND_URL=http://localhost:5000

# Add this to your actual .env.local file (not committed to git)
# Make sure your backend URL matches your backend server